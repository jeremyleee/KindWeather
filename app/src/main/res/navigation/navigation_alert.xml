<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/navigation_alert"
    app:startDestination="@id/alertListFragment">

    <fragment
        android:id="@+id/alertListFragment"
        android:name="com.tragicfruit.weatherapp.screens.alert.fragments.list.AlertListFragment"
        android:label="AlertListFragment" >
        <action
            android:id="@+id/actionOpenAlertDetails"
            app:destination="@id/alertDetailFragment" />
    </fragment>
    <fragment
        android:id="@+id/alertDetailFragment"
        android:name="com.tragicfruit.weatherapp.screens.alert.fragments.detail.AlertDetailFragment"
        android:label="AlertDetailFragment" >
        <argument
            android:name="alertId"
            app:argType="string" />
    </fragment>

</navigation>